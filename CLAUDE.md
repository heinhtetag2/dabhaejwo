# 답해줘 (dabhaejwo)

업체가 홈페이지에 스크립트 한 줄을 붙이면, 그 사이트를 학습한 챗봇이 방문자 질문에 답해주는 멀티 테넌트 SaaS.

- 프로젝트 성격: 풀스택 웹 (멀티 테넌트 SaaS) — API 서버 1 + 웹 콘솔 2 + 임베드 스크립트 1
- 스택: PostgreSQL(pgvector) + Spring Boot / Next.js(FSD) / Vite library — 선택 이유는 [docs/intake.md](docs/intake.md)

> ⚠️ 상위 `e:\_tago_product\CLAUDE.md` 는 **FinBridge 라는 다른 프로젝트**의 정책 파일이다.
> 이 프로젝트에는 적용되지 않는다 (그쪽은 MySQL·Ant Design·Maven). 이 파일이 dabhaejwo 의 기준이다.

## 구조

```
dabhaejwo/
├── CLAUDE.md · HARNESS.md · .env.example
├── .claude/rules/          ← 적용 규칙 (아래 규칙 섹션)
├── docs/
│   ├── intake.md           ← 기획서 분석·스택 결정 근거
│   ├── IMPROVEMENTS.md     ← 개선 백로그
│   ├── plan/               ← 기획서 (진실 공급원)
│   ├── prototype/          ← 시각 참조 전용. 마크업·인라인 스타일 복사 금지
│   └── architecture/
│       ├── api-contracts.md    ← 계약 단일 진실 공급원
│       └── llm-provider.md     ← 공급사 추상화 설계
│   └── deploy/README.md    ← 배포 절차·CI 변수·알고 배포하는 것
├── .gitlab-ci.yml          ← verify → deploy (서버 shell 러너)
├── deploy/
│   ├── docker-compose.yml  ← api·admin·tenant 3개 서비스
│   └── apache/             ← vhost 4개 (WS Upgrade·정적 위젯 포함)
├── api/        Spring Boot · Java 21 · Gradle KTS · PostgreSQL
├── admin/      Next.js — 운영 콘솔 (내부용)
├── tenant/     Next.js — 업체 대시보드
└── widget/     Vite library — 임베드 위젯
```

각 프로젝트에 `Dockerfile` 이 있다. **위젯의 것만 서버가 아니라 산출물 추출용이다** —
`w.js` 는 컨테이너로 띄우지 않고 Apache 가 디스크에서 서빙한다.

## 검증 명령

프로젝트 하나가 그린이 된 뒤 다음으로 넘어간다. 깨진 채로 마일스톤 완료 기록 금지.

| 대상 | 명령 |
|------|------|
| api | `./gradlew build` |
| admin | `npm run lint && npm run typecheck && npm run build` |
| tenant | `npm run lint && npm run typecheck && npm run build` |
| widget | `npm run typecheck && npm test && npm run build && npm run size` |

## 규칙 (모든 작업에 적용)

@.claude/rules/workflow-rules.md
@.claude/rules/security-rules.md
@.claude/rules/api-contract-rules.md
@.claude/rules/backend-spring-boot.md
@.claude/rules/frontend-rules.md
@.claude/rules/fsd-rules.md
@.claude/rules/widget-embed-script.md

운영 규칙(마일스톤, 세션 간 상태): @HARNESS.md

### 이 프로젝트에서 절대 흔들리면 안 되는 것

1. **모든 LLM 호출은 `global/llm/LlmGateway` 를 지난다.** Provider 를 직접 주입받아 호출하는 코드 금지. Gateway 에서만 `ai_usage` 를 적재하므로 원가 누락이 구조적으로 불가능해진다. 여기 구멍이 나면 나중에 복구할 수 없다 — 과거 원가는 다시 만들어낼 수 없기 때문이다.
2. **테넌트 격리.** 테넌트 소유 엔티티는 전부 `tenant_id` 를 갖고, 조회는 항상 현재 테넌트 컨텍스트로 제한한다. 타 테넌트 데이터 접근은 P0.
3. **감사 기록은 불변.** `audit_logs` 수정·삭제 금지, 3년 보존. 사유 필수 액션은 사유 없으면 서비스 레이어에서 거부한다.
4. **원가는 호출 시점 단가로 확정 저장.** `model_prices` 수정이 과거 `ai_usage` 에 소급되면 지난달 정산이 틀어지고 언제부터 적자였는지 추적할 수 없게 된다.

## 핵심 결정 (Decisions)

| 날짜 | 결정 | 사유 |
|---|---|---|
| 2026-08-03 | **PostgreSQL 단일 DB (pgvector 포함)** | 별도 벡터 DB를 두면 테넌트 격리·백업·해지 시 삭제를 두 시스템에서 각각 보장해야 한다. 기획서 DDL도 이미 PG 문법. **한 번 MariaDB 11.8 로 갔다가 되돌렸다 — 아래 각주 참조** |
| 2026-08-03 | **임베딩 차원 `vector(1536)`** | 모델 교체 시 전체 재임베딩이 필요 — 되돌리기 비쌈. 문서 24만 건 기준 재학습 비용 발생 |
| 2026-08-03 | **LLM 공급사 추상화, 단가는 DB** | 공급사 가격 변동·환율 대응. 코드에 모델명·단가 하드코딩 금지 |
| 2026-08-03 | **인증 3종 분리** (`/api/ops`, `/api/app`, `/api/widget`) | 운영자·업체 담당자·방문자는 신뢰 수준이 전혀 다르다. 토큰과 필터 체인을 나눈다 |
| 2026-08-04 | **공개 영역을 4번째 경계로 추가** (`/api/public`, GET 전용) | 요금제 소개는 주체가 없어 위 셋 어디에도 안 속한다. **쓰기는 두지 않는다** — 가입은 인증을 만들어내는 행위이므로 `/api/auth` 이고 레이트 리밋도 거기 붙는다 |
| 2026-08-04 | **무료 체험 14일, 카드 없음** | 승부처가 가입 후 30분인데 카드 입력이 그 앞을 막는다. PG 도 없어 받을 방법이 없다. 남용은 체험 한도(대화 100·문서 30)와 일일 원가 상한으로 제한된다 |
| 2026-08-04 | **대시보드를 `/app` 아래로** | `/` 를 랜딩에 내준다. `/dashboard` 가 아닌 이유는 API prefix 와 같은 말을 쓰기 위해서다 — 같은 영역을 두 이름으로 부르면 코드에서 계속 번역하게 된다 |
| 2026-08-03 | **위젯 Shadow DOM 격리** | 남의 사이트 CSS와 충돌 방지. iframe 은 버블·넛지 오버레이와 반응형 크기 조절이 번거로움. 충돌이 실제로 나면 패널만 iframe 전환 |
| 2026-08-04 | **모델 단가 화면은 수정이 아니라 "새 행 등록"** | 프로토타입은 표 안 `<input>` 으로 기존 단가를 직접 고친다. 그대로 옮기면 과거 `ai_usage` 의 계산 근거가 사라진다. API 에 `PATCH`·`DELETE` 를 두지 않아 화면이 잘못 만들어질 여지 자체를 없앴다 |
| 2026-08-05 | **답변 생성 시 생각(thinking)을 끈다** | 실측: 생각 464토큰 / 답변 34토큰. 원가의 대부분이 답이 아니라 생각이었고, 생각이 `maxOutputTokens` 를 먹어 답변까지 잘렸다. 끄니 품질은 같고 답변당 원가가 74% 줄었다. 근거 문서에서 사실을 뽑는 작업이라 깊은 추론이 필요 없다 |
| 2026-08-05 | **임베딩은 토큰을 먼저 세고 나서 한다** | Gemini 임베딩 응답에는 토큰 수가 없다. 계량 없이 호출하면 `ai_usage` 에 0원이 쌓이고 과거 원가는 복원할 수 없다. `countTokens` 가 실패하면 임베딩을 하지 않는다 — **계량 못 하면 돈을 쓰지 않는다** |
| 2026-08-04 | **공급사 API 키는 콘솔에서 관리** (사용자 결정) | `.env` 뿐이면 노출된 키를 재배포 없이 못 바꾼다. 시크릿을 DB 에 두는 대가로 조건을 붙였다 — AES-256-GCM 암호화(마스터 키는 env), 원문 조회 경로 없음, `OPS_ADMIN` 전용, 사유 필수 + 감사. 이 DB 는 공유 클러스터이고 `pg_hba` 가 열려 있어(P1) 평문 저장은 특히 위험하다 |
| 2026-08-04 | **임베딩 공급사 변경은 별도 경로** | 이 값을 바꾸면 이미 학습된 조각이 전부 무효가 된다 — 다른 모델이 만든 벡터끼리는 거리를 비교할 수 없다. 안전장치 저장(PUT `/cost-guards`)에 묶으면 슬랙 토글 한 번에 전 조각이 죽는다. 문서에 학습 출처를 남겨 "무엇을 다시 학습해야 하나"를 시스템이 답한다 |
| 2026-08-04 | **측정 지점이 없는 지표는 `null`** | 오늘 화면의 응답 시간·워커 가동·벡터 DB·5xx 는 잴 곳이 없다. 0으로 채우면 "응답 0ms, 오류 0건"이라는 거짓이 되고 운영자는 정상이라고 읽는다. 화면은 "집계 없음"으로 밝힌다 |
| 2026-08-05 | **알림 소켓 인증은 첫 프레임으로** | 브라우저 WebSocket API 는 핸드셰이크에 커스텀 헤더를 못 붙인다. 남은 선택은 쿼리 파라미터인데 그러면 **액세스 토큰이 접근 로그·프록시 로그에 평문으로 남는다.** 그래서 핸드셰이크는 열어두고 연결 직후 첫 프레임에서 인증한다 — 인증 전에는 어떤 알림도 나가지 않으므로 여는 것 자체는 아무 권한도 주지 않는다 |
| 2026-08-05 | **알림 저장이 진실, 소켓은 편의** | 소켓만 쓰면 접속 중이 아닐 때 발생한 알림이 사라진다 — 한도 초과·대리 접속처럼 놓치면 안 되는 것들이라 더 그렇다. 전달 실패는 로그만 남기고 삼킨다. 이 결정 때문에 화면은 소켓 없이도 동작한다(REST 목록 + 60초 폴링) |
| 2026-08-05 | **알림 중요도·수신 역할은 종류가 정한다** | 발행 지점이 정하면 같은 사건이 곳마다 다른 중요도로 뜨고, "이 알림을 누가 받나"를 알려면 발행 코드를 전부 찾아다녀야 한다. `NotificationType` enum 한 곳에 모은다 |
| 2026-08-03 | **모노레포 금지 — 4개 독립 프로젝트** | `docs/kickoff-prompt.md` §3 고정. 공유 상수·타입은 복제하되 출처 주석으로 동기화 지점 명시 |

### 각주 — MariaDB 우회로 (2026-08-03, 되돌림)

`kickoff-prompt.md` §2.2 가 MariaDB 를 적고 있고 Vector 가 11.8 LTS 에서 GA 되어 한 번 전환했다가,
같은 날 PostgreSQL 로 되돌렸다(사용자 결정). 다시 꺼내기 전에 그때 치른 값을 보라.

- `timestamptz` 가 없어 타임존을 **타입이 아니라 규율**로 지켜야 했다 — JDBC·Hibernate·엔티티 세 곳
- 배열이 없어 `messages.source_document_ids`·`feature_flags.target_ids` 를 조인 테이블로 폈다
- 부분 인덱스가 없어 `WHERE answered = false` 인덱스를 전체 인덱스로 바꿨다
- `JSON` 이 바이너리가 아니라 경로 인덱싱이 안 된다 — `jsonb` 로 하던 조회를 포기해야 했다
- **DDL 이 트랜잭션이 아니다.** V1 이 중간에 실패해 17개 테이블이 반쯤 만들어진 채 남았고 손으로 치웠다.
  PostgreSQL 이었다면 그냥 롤백됐다

## 마일스톤

<!-- 최신이 위. HARNESS.md 규칙대로 기록. -->

- **M10 배포 파이프라인** (2026-08-05) — 서버에 올릴 수 있는 상태가 됐다
  - 참조 프로젝트(scoreshot)의 방식을 그대로 따랐다 — 서버에 등록한 shell 러너가
    **서버에서 직접 빌드**하고 레지스트리를 쓰지 않는다. TLS·라우팅은 호스트 Apache
  - **서버에는 Docker 만 깐다.** 검증 잡도 컨테이너 안에서 돈다
    (`docker run --rm -v $PWD/api:/app eclipse-temurin:21-jdk ./gradlew build`).
    런타임을 서버에 깔기 시작하면 "이 서버에서만 되는 빌드"가 생긴다
  - **위젯만 컨테이너가 아니다.** 남의 사이트가 `<script src>` 로 불러가는 파일 하나라
    서버를 띄울 이유가 없다. Dockerfile 은 산출물 추출용이고 CI 가 `docker cp` 로 꺼내
    Apache 문서 루트에 **원자적으로 교체**한다(임시 폴더 → `mv`). 문서 루트에 바로 쓰면
    복사 도중의 반쪽짜리 파일이 방문자에게 나간다
  - **실호출 없이 발견한 결함 셋** — ① `build/libs/*.jar` 이 부트 jar 와 plain jar
    **두 개에 매칭돼** COPY 가 실패할 상태였다(`jar` 태스크 비활성화). ② `.env.example` 의
    `NEXT_PUBLIC_WIDGET_CDN_URL` 은 **아무 코드도 읽지 않는 이름**이었다.
    ③ 아래 XFF 건
  - **X-Forwarded-For 위조 구멍을 막았다.** `ClientIp` 가 **첫 항목**을 쓰고 있었는데,
    프록시는 들어온 헤더를 지우지 않고 실제 IP 를 **뒤에 덧붙인다** — 즉 프록시 뒤에
    놓는 순간 클라이언트가 보낸 위조값이 채택된다. 헤더만 바꿔가며 보내면
    가입 제한·OTP 재발송·방문자 질문 제한이 전부 무력화된다. **마지막 항목**으로 바꾸고
    Apache 에서도 들어온 헤더를 버리게 했다(양쪽 방어). 테스트 5개로 고정
  - 하드코딩 도메인 3곳을 빌드 인자로 뺐다 — 위젯의 API 주소, 설치 화면의 스크립트 주소,
    콘솔의 API 주소. `NEXT_PUBLIC_*`·`VITE_*` 는 **런타임에 못 바꾼다**(번들에 인라인)
  - Apache vhost 4개. WebSocket 블록은 **catch-all `ProxyPass` 보다 위**에 둔다 —
    아래 두면 업그레이드가 평범한 HTTP 로 프록시돼 핸드셰이크가 400 으로 끝난다
  - **Docker 가 로컬에 없어 이미지 빌드는 검증하지 못했다.** 대신 Dockerfile 이 기대하는
    산출물을 실제 빌드로 확인했다 — 부트 jar 1개 · `.next/standalone/server.js` ·
    `.next/static` · `dist/w.js`(gzip 9.5KB), 그리고 세 빌드 인자가 번들에 실제로 박히는 것
  - `./gradlew build` 그린(테스트 +5) · admin·tenant·widget 게이트 그린
  - **새로 등록한 P0** — `OPS_IP_ALLOWLIST` 는 필수 변수인데 **읽는 코드가 없다.**
    운영 콘솔이 사실상 인터넷에 열린다. 배포 전 Apache `Require ip` 주석을 반드시 풀 것

- **M9 알림** (2026-08-05) — 찾아가지 않아도 알게 된다. 운영·업체 양쪽 + WebSocket
  - 기획서 `docs/plan/notification-plan.md` 신규. 계약 `api-contracts.md` §11
  - V10 `notifications` — 수신자(`audience`+`tenant_id`)·역할 배열·`target_path`·
    `read_at`·`dedupe_key`. 부분 유니크 인덱스가 중복을 막는다
  - **15종.** 운영 7(가입·문의·전체 상한·업체 상한·원가 초과·체험 종료·학습 실패 누적) +
    업체 7(대리 접속·한도 80%/소진·체험 종료·학습 완료/실패·연락처·답변 공백) + 결제 1은 stub
  - **중요도와 받을 역할은 종류가 정한다**(`NotificationType`). 발행 지점이 정하면 같은 사건이
    곳마다 다른 중요도로 뜬다. CS 에게 원가율, 개발에게 결제는 소음이라 역할로 좁힌다
  - 발행 경로는 **하나**(`NotificationPublisher`). 흩으면 어떤 건 저장만, 어떤 건 푸시만 된다
  - **푸시는 커밋 뒤에.** 롤백된 트랜잭션의 알림이 화면에 뜨면 눌러서 없는 대상으로 가고
    새로고침하면 사라진다. 원가 상한만 반대로 `REQUIRES_NEW` — 그 요청은 거절되며 롤백되는데
    같이 되감기면 "상한에 도달했다"는 사실까지 사라진다
  - 즉시 발행(가입·문의·대리 접속·리드·학습 종료) / 매시 훑기(`NotificationSweeper`, 20분 —
    일 집계가 정각이라 그 뒤를 본다). 훑기 중복은 `dedupe_key` 가 막는다
  - **토큰을 URL 에 싣지 않는다.** 브라우저 WebSocket 은 헤더를 못 붙이지만 쿼리로 보내면
    액세스 토큰이 접근 로그에 남는다 — 연결 뒤 **첫 프레임**으로 받고 그 전엔 아무것도 안 보낸다.
    구독 대상은 토큰에서만 유도한다(본문의 테넌트 id 를 받지 않는다)
  - **대리 접속은 업체 알림을 읽음 처리할 수 없다** — "운영팀이 접속했습니다"를 그 운영자가
    지우는 셈이 된다. 남의 알림은 404(존재 여부를 알려주지 않는다)
  - `?tenantId=` 딥링크가 실제로 그 업체를 연다 — 업체 화면의 선택을 URL 로 옮겼다.
    알림이 이름만 알려주고 다시 찾게 만들면 알림이 아니라 알림에 대한 안내다
  - 실 DB 실호출 검증 — 가입→ops 알림 적재 / 위젯 리드→업체 알림 200·`targetPath` 확인 /
    읽음 204→안읽음 0 / **타 수신자 알림 404**(격리) / 무토큰 403 /
    **WS 핸드셰이크→AUTH→READY→실시간 NOTIFICATION 수신**, 잘못된 토큰은 즉시 종료(1003)
  - `./gradlew build` 그린(테스트 +5) · `admin`·`tenant` lint·typecheck·build 그린
  - **한계** — 소켓 세션이 인스턴스 메모리에만 있다. 여러 대가 되면 실시간이 안 가지만
    알림은 DB 에 남아 목록엔 보인다. 메일·슬랙으로 나가지 않는다(IMPROVEMENTS P1)

- **관리자 계정 관리** (2026-08-05) — 콘솔에서 운영자를 등록·수정·비활성화한다
  - 스키마 변경 없음. `operators` 는 V1 부터 필요한 것을 다 갖고 있었고(role·active·
    password_hash) 화면이 없었을 뿐이다. V8 은 감사 액션 `OPERATOR_WRITE` 추가와 인덱스뿐
  - **삭제를 만들지 않았다.** `operators` 를 여섯 테이블이 FK 로 참조하고 그중 `audit_logs` 는
    수정·삭제 불가에 3년 보존이다 — 지우면 "누가 이 업체에 대리 접속했나"의 답이 사라진다.
    비활성화가 그 자리를 대신하고, 비활성 계정도 목록에 남긴다(숨기면 왜 로그인이 안 되는지 모른다)
  - **개인별 권한을 두지 않았다.** 권한은 역할이 정하고 매핑은 코드에 있다(기획서 §7).
    개인별로 열면 누군가 자기에게 `AUDIT_READ` 를 붙일 수 있고 감사 체계가 무의미해진다.
    대신 `GET /role-permissions` 로 **서버가 코드의 진실을 그대로 내려주고** 화면이 그걸 보여준다 —
    표를 프론트에 복제하면 배포 후 실제와 어긋난다
  - **잠금 방지** — 자기 역할 낮추기·자기 비활성화 거부, 마지막 활성 `OPS_ADMIN` 강등·비활성화 거부.
    그 상태가 되면 화면으로 되돌릴 수 없고 DB 를 직접 고쳐야 한다
  - 이메일은 등록 후 불변(감사 기록이 가리키는 사람이다). 초기 비밀번호는 관리자가 정한다 —
    초대 메일이 아직 stub 이다
  - 실 DB 실호출 검증 — 목록·역할 매트릭스 200 / CS 역할 403 / 자기 강등·자기 비활성화 400 /
    중복 이메일 400 / 사유 공백 400 / 등록→역할변경→비밀번호→비활성화→**로그인 401**→복구→로그인 200.
    관리자가 2명일 때는 강등·비활성화가 정상 동작(보호 규칙의 거짓 양성 없음).
    검증 계정 정리 완료, `OPERATOR_WRITE` 감사 9건은 불변이라 유지
  - `./gradlew build` 그린 · `admin` lint·typecheck·build 그린(14라우트)

- **M7 Gemini 실연동** (2026-08-05) — 챗봇이 진짜 모델로 답한다
  - 사용자 키를 **콘솔 경로로 등록**(암호화 저장). `.env` 는 비운 채로 둔다
  - **실호출이 아니면 못 잡았을 결함 4개를 잡았다.** 문서만 보고 만든 어댑터는 전부 통과했었다
  - ① **임베딩 원가가 0원으로 적재되고 있었다** — `batchEmbedContents` 응답에 `usageMetadata`
    가 없다(단건 `embedContent` 도 마찬가지). `countTokens` 를 **먼저** 부르고 실패하면
    임베딩을 하지 않는다 — 계량 못 하면 돈을 쓰지 않는다. 실측 확인: 수정 전 `in=0/0원`,
    수정 후 `in=280/0.0588원`
  - ② **질문과 문서의 임베딩 공급사가 갈렸다** — 문서는 설정값(GOOGLE), 질문은 환경변수(STUB).
    다른 모델의 벡터끼리는 거리가 무의미해 **검색이 조용히 아무것도 못 찾았다.** 오류도 로그도
    없이 "답변 실패"로만 보이는 종류다. `EmbeddingSettings` 한 곳으로 합쳤다
  - ③ **유사도 임계값 0.72 가 전부 거절하는 값이었다** — 해시 임베딩 시절 값이다. 실측하니
    관련 질문 0.65~0.69 / 무관 0.45~0.50 이라 **0.575** 로 재보정(설정값이라 재배포 없음)
  - ④ **생각 토큰이 답변을 잘라먹고 원가를 삼켰다** — Gemini 3 계열은 기본으로 생각한다.
    400 예산에서 생각이 380을 가져가 답변이 `MAX_TOKENS` 로 잘렸다. `thinkingBudget: 0` 으로
    끄니 **품질은 같고 답변당 원가 10.0원 → 2.61원(74%↓)**
  - 곁가지 — 필수 파라미터 누락이 500 이었다(400 으로). 클라이언트 실수가 5xx 그래프를 더럽힌다.
    프롬프트 문자열에 javadoc `<b>` 태그가 섞여 모델에 그대로 가고 있었다
  - **실 DB 전 구간 확인** — 문서 업로드 → 실 Gemini 학습(1536차원) → 검색 → 위젯 질문 →
    한국어 답변(금액·조건이 원문과 일치, 업체 말투·출처 반영) → `ai_usage` 적재.
    무관한 질문은 모델을 부르지 않고 거절. 새 호출 중 **원가 0원 적재 0건**
  - 시드 모델명은 **전부 실재했다**(`gemini-3.5-flash`·`-lite`·`gemini-3.1-flash-lite`·
    `gemini-embedding-001`). 어제 "GA 목록과 어긋난다"고 적은 P1 은 틀렸다 — 문서의 GA 목록보다
    키가 실제로 쓸 수 있는 범위가 넓다. 항목 삭제
  - `./gradlew build` 그린(테스트 79개) · `admin` 게이트 그린

- **M6 공급사 연결** (2026-08-04) — 실 모델을 붙일 수 있는 상태가 됐다
  - **API 키를 운영 콘솔에서 등록·교체한다**(사용자 결정). 그전에는 `.env` 뿐이라 노출된 키를
    재배포 없이 갈아끼울 방법이 없었다 — P0 가 풀리지 않던 이유다
  - 시크릿을 DB 에 두는 것은 `security-rules` 의 "시크릿은 환경변수"에서 벗어난다. 조건을 걸었다:
    **AES-256-GCM 암호화**(마스터 키만 env) · **원문을 되읽는 경로 없음**(마스킹 힌트만) ·
    `OPS_ADMIN` 전용 · 사유 필수 + 감사(키도 힌트도 기록하지 않음) · `DELETE` 없음(끄기만)
  - `GoogleLlmProvider` — `generate` + `batchEmbedContents`. 키는 **헤더**로 보낸다(쿼리로 보내면
    접근 로그에 남는다). 연결 5초·읽기 30초 타임아웃 — 공급사가 멈추면 방문자 요청이 물린다.
    임베딩은 3072 를 자르지 않고 **처음부터 1536 으로 요청**한다
  - 임베딩 공급사를 환경변수에서 **설정값으로**(`cost_guards.embedding_provider`).
    답변 공급사는 이미 DB(`plan_model_assignments`)였는데 임베딩만 재배포가 필요했다
  - **바꾸면 조각이 무효가 된다**는 사실을 시스템이 안다 — 문서에 학습 출처를 남기고
    (`embedding_provider`·`embedding_model`), 업체의 "다시 학습"이 실패분뿐 아니라
    출처가 다른 문서까지 대상으로 삼는다. 안전장치 저장과 **경로를 나눴다**
  - 실 DB 검증 — V7 적용, 등록 시 DB 에 **평문 없음**(암호문 84자) · 감사 meta 에 키 없음 ·
    서버 로그에 키 없음 · CS 역할 403 · 사유 공백 400 · 끄면 환경변수로 안 돌아감.
    검증용 키·설정 전부 정리(감사 기록은 불변이라 남김)
  - `./gradlew build` 그린(테스트 77개) · `admin` lint·typecheck·build 그린
  - **여기서 멈춘 곳** — 유효한 Gemini 키가 없어 **어댑터가 실제 Gemini 호출로 검증되지 않았다.**
    요청·응답 형상은 공식 문서로 확인했으나 실호출은 키 등록 후에 해야 한다.
    또 시드의 `gemini-3.5-flash` 는 현재 GA 목록에 없다(GA: `gemini-3.6-flash`,
    `gemini-3.5-flash-lite`) — 모델명이 DB 에 있으므로 콘솔에서 단가 등록 + 배정 변경으로 해결한다

- **M6 인증 강화** (2026-08-05) — 로그인 2단계·초대 링크·비밀번호 찾기
  - **SMTP 연동** — `Mailer` + `SmtpMailer`. 미설정이면 `UnavailableMailer` 가 명시적으로 거절한다.
    로컬 로그 출력으로 대체하지 않는다 — 개발에서만 되는 척하면 "메일이 안 온다"가 배포로 미뤄진다
  - **로그인이 두 단계다**(업체·운영 콘솔 둘 다). 비밀번호가 맞아도 토큰이 안 나오고,
    메일로 간 6자리 코드를 맞혀야 나온다. 코드는 **DB 에 해시로만** 있고, 5분 만료·5회 시도·
    새로 발급하면 이전 코드 전부 폐기. 챌린지에 `APP`·`OPS` 스코프를 못 박아
    **업체 챌린지로 운영자 토큰을 받아낼 수 없다**(실호출로 확인 — 401)
  - **팀원 초대가 실제로 동작한다** — 이름·이메일·권한·전화번호를 받아 초대 링크를 메일로 보낸다.
    링크는 32바이트 난수이고 **DB 에는 SHA-256 해시만** 남는다. 7일·1회. 수락 화면에서
    비밀번호를 정하면 바로 로그인할 수 있다. **메일 발송 실패는 팀원 행까지 롤백한다** —
    행만 남기면 초대한 사람은 상대가 못 받은 사실을 모른다
  - **비밀번호 찾기** — 임시 비밀번호(12자)를 메일로 보내고 **기존 비밀번호를 덮어쓴다**.
    임시 비밀번호로는 로그인이 끝나지 않고(`PASSWORD_CHANGE_REQUIRED`) 재설정 화면으로만 간다.
    없는 주소도 204 — 응답이 갈리면 가입 여부 확인 도구가 된다
  - 스키마 V9 — `login_challenges`, `tenant_members.phone`·`invite_token_hash`·
    `must_change_password`, `operators` 임시 비밀번호 컬럼
  - 화면 — tenant 로그인(2단계)·비밀번호 찾기·초대 수락, 팀원 초대 폼(전화번호·재발송),
    admin 로그인(2단계)·비밀번호 찾기
  - 중복 제거 — IP 해시 계산이 세 곳에 흩어져 있던 것을 `ClientIp` 로,
    슬라이딩 윈도 제한이 두 벌이던 것을 `SlidingWindowLimiter` 로
  - **실 SMTP·실 사서함으로 전 구간 확인**(31개 통과) — 인증 코드·초대 링크·임시 비밀번호가
    실제로 도착하고, 코드 재사용·링크 재사용·임시 비밀번호 재사용이 전부 거부된다.
    운영 콘솔 OTP 도 실메일로 확인
  - `api` build 그린 · `tenant`·`admin` lint·typecheck·build 그린
  - 메일 본문은 **HTML + 평문을 함께** 보낸다(`MailTemplate`). HTML 을 막는 클라이언트에서도
    코드가 읽혀야 한다. 업체명·이름이 본문에 들어가므로 **반드시 이스케이프**하고,
    스타일은 전부 인라인이다 — 메일 클라이언트는 `<style>` 블록을 지운다
  - **개발 우회**(`OTP_DEV_ACCEPT_ANY`) — 켜면 형식만 맞는 6자리면 통과한다. 메일은 그대로 나간다.
    `production` 프로파일에서 켜져 있으면 `DevOtpGuard` 가 **기동을 막는다** —
    조용히 꺼주면 아무도 설정이 잘못된 줄 모른다
  - 대시보드 입력 컨트롤을 `shared/common/control` 로 모았다 — 세로 여백이 다섯 종류라
    **같은 줄의 입력과 버튼 높이가 어긋나 있었다.** 팀원 표의 액션 칸이 좁아 버튼이
    세로로 접히던 것도 함께
  - **운영자 시드 주소를 설정으로 뺐다**(`OPS_SEED_EMAIL`) — `@dabhaejwo.com` 은 없는 주소라
    2단계가 붙은 뒤로는 코드를 받을 수 없어 로그인 자체가 막힌다

- **공개 영역 UI 개편** (2026-08-04) — 랜딩·요금제·가입·로그인·약관·개인정보 6화면
  - **색은 그대로, 레이아웃만 바꿨다**(사용자 결정) — ink·mark 팔레트를 유지해 랜딩에서 본 색과
    로그인 후 색이 같다. 다르면 다른 서비스처럼 보인다
  - 공개 영역 전용 토큰 — `fill`·`fill-2`·`edge`·`code`·`mark-ink` 색과
    `panel`(22px)·`block`(16px)·`btn`(14px) 곡률. **대시보드 토큰은 건드리지 않았다**:
    읽는 화면과 다루는 화면은 필요한 밀도가 다르다
  - **테두리 대신 면으로 구분한다.** 헤어라인을 걷어내고 옅은 배경(`fill`)과 여백으로 나눈다
  - 타이포 — 히어로 52px / 섹션 32px, `tracking` 을 -0.035~-0.045em 로 조이고 본문을 17px·1.75 로 키웠다
  - `Button` 에 `lg` 사이즈 추가(admin 사본도 같이). `Notice` 에 `size` 추가 — 기본값은 그대로라
    대시보드 화면은 바뀌지 않는다
  - 중복 제거 — 로그인·가입이 각자 갖고 있던 입력 필드를 `shared/ui/form-field` 로 모았다.
    한쪽만 고치면 두 화면이 어긋난다
  - 하드코딩 색 정리 — `#8a6a00` 이 3곳에 흩어져 있던 것을 `mark-ink` 토큰으로
  - `tenant` · `admin` 둘 다 lint·typecheck·build 그린. 6화면 서버 렌더링 확인

- **M5 챗봇이 답한다** (2026-08-04) — 방문자 질문 → 근거 검색 → 답변 → 기록까지 이어졌다
  - `AnswerService` — **근거를 먼저 찾고, 근거가 있을 때만 모델을 부른다.** 최고 유사도가
    `cost_guards.answer_fail_similarity` 에 못 미치면 모델을 호출하지 않고 실패로 끝낸다.
    근거 없이 부르면 모르는 것을 지어내고 그 답에 돈까지 나간다
  - `PromptBuilder` 는 순수 함수다(테스트 4개). 프롬프트를 보려고 실제 호출을 해야 하면
    확인할 때마다 돈이 나간다
  - `/api/widget/**` 5종 — session·ask·faq·feedback·lead. 테넌트는 **필터가 검증한 키에서만**
    온다. 본문에서 받으면 남의 업체를 사칭해 원가를 태울 수 있다
  - `ChatGuard` — IP 분당 질문 수 → 월 대화 한도 → 일일 원가 상한(업체별 + 전체) 순으로 본다.
    **싼 검사부터** 두어야 봇이 두드릴 때마다 DB 를 긁지 않는다
  - 저장 답변(공통 질문)은 모델도 `ai_usage` 도 거치지 않는다 — 실호출로 확인함
    (`ANSWER` 2건은 위젯 질문·미리보기뿐, 공통 질문 클릭은 0건)
  - `answer_gaps` 자동 적재 — 답변 실패는 `ANSWER_FAILED`, 👎 는 `THUMBS_DOWN`.
    **실패한 답변에 👎 를 눌러도 횟수를 올리지 않는다** — 한 번 물은 질문이 두 번으로 세어진다
  - `messages.source_document_ids` 적재 — "왜 이렇게 답했나"의 유일한 기록. 조각은 재학습하면
    바뀌고 프롬프트는 어디에도 남지 않으므로 지금 안 남기면 나중에 못 만든다
  - 위젯 UI 완성 — 👍👎, 답변 실패 시 연락처 폼, 근거 링크. **gzip 9.5KB**(예산 30KB)
  - 대시보드 미리보기 자유 입력 연결 — 위젯과 **같은 파이프라인**. 대화를 만들지 않아 통계가
    오염되지 않지만 **원가는 진짜로 나간다**
  - 고친 것 둘 — ① 질문·답변을 각각 `now()` 로 찍어 **대화 로그에 답이 질문 위에 나왔다**.
    답변 시각을 질문 시각 + 걸린 시간으로 바꿨다 ② 없는 경로가 500 이었다(404 로).
    오타 하나가 서버 오류로 보고되면 5xx 그래프를 못 믿는다
  - **실 DB 전 구간 확인** — 학습(조각 2) → 위젯 세션 → 자유 질문(`answered`, latency 74ms,
    근거 1건) → 저장 답변(`saved`) → 👎 → 연락처 → 개선 목록 → 미리보기 → 대화 로그.
    `ai_usage` 에 `EMBED_DOC` 1 · `EMBED_QUERY` 2 · `ANSWER` 2. 무키·가짜키 403.
    검증용으로 바꾼 설정 복구 및 데이터 정리 완료
  - `./gradlew build` 그린(테스트 71개) · `tenant` · `widget` 전부 그린
  - **여기서 멈춘 곳** — 공급사가 `STUB` 이라 답변 문구가 `"안내를 준비 중입니다. (stub 응답)"`
    이고 임베딩이 해시 기반이라 검색 순위에 의미가 없다. 요금제 배정은 `GOOGLE` 을 가리키는데
    키가 없어 **실제로는 `LLM_PROVIDER_UNAVAILABLE` 로 거절된다.** 조용히 STUB 으로 떨어뜨리지
    않은 것이 의도다 — 그러면 운영에서 공급사가 끊겼을 때 아무도 모른다

- **M4 지식 학습 파이프라인 완료** (2026-08-04) — 올린 파일이 실제로 검색된다
  - `TextExtractor`(PDFBox 3 · POI 5) → `Chunker`(600자 · 90자 겹침, 문단→문장→공백 경계)
    → `LlmGateway.embed` → `KnowledgeChunkRepository`(pgvector native). 조각 저장·검색은
    이 저장소 하나만 `<=>` 를 안다
  - `IndexingWorker` 가 `PENDING` 문서를 10초마다 최대 5건 집어간다. 업로드 요청은 즉시 반환하고,
    화면은 **진행 중인 문서가 있을 때만** 5초 폴링한다 — 응답은 언제나 `PENDING` 이므로
    다시 읽지 않으면 화면이 영원히 "대기 중"으로 남는다
  - `GET /api/app/knowledge/search` — 테넌트 격리 유사도 조회(기본 5 · 최대 20).
    `retry-failed` 는 `FEATURE_NOT_READY` 에서 **실제 재학습**으로 전환, 되돌릴 게 없으면 400
  - 실패를 삼키지 않는다 — `pdf_no_text`(스캔본) · `no_text_found` · `parse_failed` ·
    `indexing_failed` · `no_original_file` 을 문서에 남기고 화면이 그대로 보여준다
  - **실 DB·실 R2 전 구간 확인** — 업로드 → `INDEXED`(조각 2) → 검색 → 삭제 시 조각 제거.
    `ai_usage` 에 `EMBED_DOC` 1 · `EMBED_QUERY` 2 적재됨을 DB 에서 직접 확인. 검증 데이터 정리 완료
  - `./gradlew build` 그린(테스트 68개) · `tenant` lint·typecheck·build 그린
  - **아직 답을 만들지는 않는다.** 검색 결과로 답변을 생성하는 단계와 위젯 연결이 남았다
  - 한계는 `docs/IMPROVEMENTS.md` P1 — 단일 인스턴스 전제, 자동 재시도 없음, 파일 전량 메모리 적재

- **M3 운영 콘솔 완료** (2026-08-04) — 원가를 볼 수 있는 눈이 생겼다
  - 기획서 `docs/plan/admin-console-plan.md` + `admin-console-tenant-plan.md`
  - 스키마 V4 — `operators.password_hash`(**없어서 아무도 콘솔에 못 들어갔다**),
    감사 액션 4종 확장(`ACTIVATE`·`PLAN_WRITE`·`FLAG_WRITE`·`TICKET_WRITE`), 조회 인덱스 2개
  - `/api/ops/**` **31개 엔드포인트** + `/api/auth/ops/login`.
    업체(목록·필터 건수·상세·활동 이력·상태/요금제/체험/쿼터/메모), 대리 로그인(발급·연장·종료),
    오늘, 수익성, AI 사용량 4종, 작업 큐, 요금제, 모델 단가, 안전장치, 요금제별 모델 배정,
    기능 공개, 문의, 감사 기록
  - **일 집계 배치**(`DailyUsageAggregator`, 매시 정각) — `ai_usage`·`conversations`·`messages`
    → `tenant_daily_usage`. 어제 것도 함께 돌려 자정 경계 구멍을 메운다. 증분이 아니라
    **다시 계산해 덮는다**(원장이 진실, 이 테이블은 캐시)
  - `/api/auth/refresh` 는 하나로 두고 토큰의 `scope`(ops·app)로 분기 — 운영자 리프레시
    토큰으로 업체 액세스 토큰을 받아내는 경로가 없다
  - `admin` 10화면 + 로그인. **lint/typecheck/build 그린**, api **build 그린(테스트 67개)**
  - 실 DB 실호출로 **17개 조회 200 / 쓰기 경로 정상 / 권한 매트릭스 서버 강제 확인**
    (CS→감사·단가·수익성 403, 영업→대리 로그인 403, 업체 토큰→ops 401, 사유 공백→400)
  - **프로토타입과 의도적으로 다르게 만든 곳** — 모델 단가를 표 안에서 고칠 수 없다.
    프로토타입은 `<input>` 직접 수정이지만 그대로 옮기면 소급 변경이 된다.
    "새 단가 등록 + 적용 시점"만 있고 기존 행은 읽기 전용 이력이다
  - 목록 정렬 버그 수정 — 원가율은 집계값이라 SQL 정렬이 안 되는데 페이지 단위로 잘라온 뒤
    메모리 정렬을 하고 있었다. 2페이지 첫 줄이 1페이지 끝줄보다 원가율이 높은 상태였다
  - **비어 있는 것** — 답변 파이프라인이 없어 `ai_usage`·`jobs` 가 비었다. 수익성·AI 사용량·작업 큐는
    실 엔드포인트에 빈 상태다. 오늘 화면의 응답 시간·워커·벡터 DB·5xx 는 **측정 지점이 없어 `null`**
    (화면은 "집계 없음"). 0으로 채우지 않은 것이 의도다 — 0이면 정상으로 읽힌다

- **M2 공개 페이지 완료** (2026-08-04) — 가입 경로가 열렸다
  - 기획서 `docs/plan/tenant-public-plan.md`
  - 라우팅 재배치 — `/` 랜딩, 대시보드는 `/app` 아래로. 총 17개 라우트
  - 공개 화면 — 서비스 안내·요금제·가입·로그인·약관·개인정보처리방침 (전부 Server Component)
  - `/api/public/plans`(GET 전용), `/api/auth/app/signup`(원자적 + IP 레이트 리밋),
    `/api/app/plan/upgrade-request`(→ `tickets`)
  - **미완** — 약관 본문, 사업자 정보(전자상거래법 표기 의무), 카드 결제. 전부 P0/stub 로 등록
  - api **build 그린**, tenant **lint/typecheck/build 그린**

- **M1 업체 대시보드 완료** (2026-08-04) — 10화면 전부 실 API 연동
  - 기획서 `docs/plan/tenant-plan.md` 신규 확보. 기존 `tenant-plan.md` 는 운영 콘솔의
    "업체" 메뉴 문서였고 `admin-console-tenant-plan.md` 로 개명했다
  - 스키마 V2·V3 — `password_hash`, `bot_settings`, `answer_gaps`,
    `faqs.follow_up_faq_ids`, `bot_settings.avatar_url`, `answer_gaps.question_embedding`,
    `messages.latency_ms`
  - `/api/app/**` 13개 엔드포인트. 인증(로그인·리프레시), 홈 집계, 공통 질문 CRUD·정렬,
    지식 소스·문서, 답변 개선(승격/넘어가기), 대화 로그, 연락처(CSV), 챗봇 설정,
    허용 주소, 요금제, 팀원, 운영팀 접속 이력
  - 권한 3단계를 서버가 강제 — 조회는 전 역할 / 편집은 OWNER·EDITOR /
    팀원·결제는 OWNER. 대리 접속 토큰은 VIEWER 로 발급돼 운영자가 설정을 못 바꾼다
  - `tenant` 10화면 + 로그인. **lint/typecheck/build 그린**, api **build 그린(테스트 45개)**
  - 실 DB 실호출로 전 엔드포인트 200 / 무토큰 403 확인
  - **답변 파이프라인(RAG)이 없어 비어 있는 것** — 미리보기 자유 입력, `answer_gaps`
    자동 적재, `messages.latency_ms`, 크롤러·임베딩 워커(`FEATURE_NOT_READY` 로 명시 거절)

- **M0 부트스트랩 완료** (2026-08-03) — 4개 프로젝트 전부 검증 그린
  - 문서 정리·인테이크·하네스 조립·API 계약·DB 스키마(V1, 26개 테이블)
  - `api` — Spring Boot 4.1 / Java 21 / Gradle 9.5.1. `LlmGateway` + 원가 파이프라인,
    인증 3종 분리, `@RequirePermission` AOP, 감사 기록, 업체 목록 엔드포인트.
    **`./gradlew build` 그린** (단위 테스트 21개)
  - `admin` — 운영 콘솔 FSD 셸 + 10화면 라우트. **lint/typecheck/build 그린**
  - `tenant` — 업체 대시보드 FSD 셸 + 10화면 라우트 + 대리 접속 배너.
    **lint/typecheck/build 그린**
  - `widget` — Vite 8 IIFE + Preact + Shadow DOM. **typecheck/test(9)/build/size 그린**,
    gzip 8.86KB (예산 30KB)
  - 환경 제약 — Docker 없음(통합 테스트 불가, IMPROVEMENTS P1), Gradle 없음(Initializr 공식 래퍼 사용)
  - **실 DB 로 기동 확인** (2026-08-04) — PostgreSQL 16.14 + pgvector 0.6.0.
    Flyway `v1 init success=true`, 26개 테이블 + `flyway_schema_history`,
    감사 불변 트리거 2개, `knowledge_chunks_embedding_idx`(HNSW) 생성.
    seed 적재(plans 5 · model_prices 7 · plan_model_assignments 5 · cost_guards 1) 및 한글 정상.
    **`ddl-auto: validate` 통과** — `uuid`/`jsonb`/`timestamptz` 매핑 확인.
    `/actuator/health` UP, 인증 3종 경로 모두 미인증 403.
    DB 서버는 개발 PC 와 같은 LAN 이라 `192.168.0.254:6001` 로 직접 붙는다 — 터널·포트포워딩 불필요
  - 다음 마일스톤 후보는 아래 "다음 마일스톤 제안" 참조

## 다음 마일스톤 제안

배관은 방문자 질문부터 원가 기록까지 전부 이어졌다. **남은 것은 그 배관에 진짜 모델을 꽂는 일이다.**

| 후보 | 내용 | 이유 |
|---|---|---|
| **M6 — 실 LLM 공급사 연동** | `GeminiProvider`(임베딩 + 생성). 키를 `.env` 에 넣고 `model_prices` 에 실단가 등록 | **지금 챗봇은 stub 문구만 답하고, 배정이 `GOOGLE` 이라 실호출은 거절된다.** 이걸 붙여야 제품이 처음으로 진짜 답을 한다. 임베딩 차원이 `vector(1536)` 로 고정돼 있어 모델 선택이 되돌리기 비싼 결정이다 |
| M7 — 웹 크롤러 | 사이트 다시 읽기(`recrawl`). robots.txt 준수·동시성 상한 | 지금은 파일 업로드로만 학습한다. 기획의 핵심 약속("스크립트 한 줄")이 아직 미완 |
| M7' — Docker 확보 후 통합 테스트 | Flyway·JPA 매핑·pgvector 검색·답변 파이프라인을 자동 검증으로 고정 | 지금은 수동 실행으로만 확인했다. 회귀를 막으려면 필요하다 |
| M8 — 결제 | `PaymentGateway` 실 연동 | 체험 14일이 끝나는 업체가 생기기 전까지 |

## 미완료 / Stub 목록

외부 의존성은 임의 구현하지 않는다. 인터페이스 + stub, `// TODO(stub):` 주석 필수, 조용한 성공 처리 금지.

| 항목 | 상태 | 필요 시점 |
|------|------|----------|
| LLM 공급사 | **GOOGLE 연동 완료**(2026-08-05) — 답변·임베딩 모두 실 Gemini. 키는 콘솔에 암호화 등록돼 있고 임베딩 공급사도 `GOOGLE` 이다. `ANTHROPIC`·`OPENAI` 는 인터페이스만 있고 어댑터가 없다 | 다른 공급사가 필요해질 때 |
| PG 결제 (**토스페이먼츠**) | 미연동. 유료 전환이 **문의 티켓**으로 접수돼 사람이 처리한다. 그래서 `PAYMENT_RECEIVED` 알림은 아무도 발행하지 않고, "결제했다"와 "문의가 왔다"가 지금은 같은 사건이다. 테스트 키만 `.env`(`TOSS_*`)에 있고 **코드는 읽지 않는다** — 운영 전 라이브 키로 교체 | 유료 전환 전 |
| 슬랙 알림 `#ops-alert` | 미구현. **알림(M9)이 콘솔 안에서만 뜬다** — 콘솔을 안 보면 원가 상한 도달도 모른다 | 운영 개시 전 |
| ~~이메일~~ | **연동 완료 (M6)** — SMTP(`SmtpMailer`). 초대·OTP·임시 비밀번호가 실제로 나간다. 미설정 시 `FEATURE_NOT_READY` 로 거절 | — |
| 한도 안내·결제 실패 메일 | 발송 수단은 있으나 **보내는 지점이 없다.** 한도 80% 도달은 M9 에서 **화면 알림**까지만 간다 — 메일로는 안 나간다 | 운영 개시 전 |
| ~~파일 저장소~~ | **연동 완료** — Cloudflare R2(S3 호환). `FileStorage` + `S3FileStorage`. 미설정 시 `FEATURE_NOT_READY` 로 거절하며 로컬 디스크로 대체하지 않는다 | — |
| SSO (운영 콘솔) | **2FA 는 완료 (M6, 메일 OTP)**. SSO 는 미구현이고 `operators.totp_secret` 은 쓰이지 않는다. **운영자 생성 경로는 `local` 시더뿐** | 운영자 3명 초과 시 |
| 작업 큐 재시도 | `POST /api/ops/jobs/{id}/retry`·`/retry-all` 이 `FEATURE_NOT_READY`(503)로 **명시적으로 거절**한다 — 큐에 다시 넣어도 집어갈 워커가 없다 | 임베딩 워커·크롤러 연결 시 |
| 세금계산서 발행 | 미구현. 문의 티켓으로 우회 | 사업자 고객 유입 시 |
| 웹 크롤러 | 미구현. `/knowledge/sources/{id}/recrawl` 이 `FEATURE_NOT_READY`(503) 로 **명시적으로 거절**한다 — 상태만 바꾸면 업체가 학습된 줄 알고 기다린다. robots.txt 준수·동시성 상한 필수 | 자동 재크롤링 오픈 전 |
| ~~임베딩 워커~~ | **완료 (M4)** — `IndexingWorker`. `retry-failed` 도 실제 재학습으로 동작 | — |
| ~~답변 파이프라인 (RAG)~~ | **완료 (M5)** — `latency_ms`·`answer_gaps` 자동 적재·미리보기 자유 입력 전부 동작 | — |
