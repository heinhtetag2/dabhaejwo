# IMPROVEMENTS — 개선 백로그

> 지금 안 고치기로 한 문제를 기록. 발견 즉시 등록, 해결 시 삭제.
> - **P0**: 런칭/배포 차단 — 새 기능보다 먼저 처리
> - **P1**: 기능 결함이지만 우회 가능
> - **P2**: 개선/리팩토링
> - 항목이 rules/하네스 자체의 문제면 비고에 `[harness]` 태그 → 마일스톤 종료 시 E:\_agent 팩토리로 환류

## P0

| 항목 | 위치 | 등록일 | 비고 |
|------|------|--------|------|
| **요금제 비교표 체크마크가 목업이다 — 실제 기능 게이팅 없음** | `tenant/src/features/pricing/ui/pricing-view-v2.tsx` `ROW_TIER_ACCESS` | 2026-08-12 | 사용자 요청으로 STARTER/BUSINESS/ENTERPRISE 별 체크·미체크(–)를 시각적으로 나눴다. **백엔드에 티어별 기능 플래그가 전혀 없다** — 지금 세 플랜은 실제로 기능이 100% 동일하고 대화·문서 한도만 다르다(이 페이지 FAQ "플랜 간 차이는 무엇인가요?"와 정면으로 모순). 이 화면은 `/pricing` 의 "Revamp" 토글로 실제 방문자가 볼 수 있다(기본은 "Original"). **v2 를 최종 디자인으로 확정하기 전에 반드시 정리할 것** — ① 실제로 티어별 기능을 가를지 결정(백엔드 플래그 필요) ② 아니면 이 표를 전부 체크로 되돌린다. 사용자 조치 필요 |
| **약관·개인정보처리방침 본문 없음** | `tenant/src/app/(public)/terms`·`privacy` | 2026-08-04 | 가입 화면이 동의를 받는데 가리키는 문서가 비어 있다. 라우트와 목차만 있고 본문은 "준비 중"이다 — **법무 검토 없이 쓴 약관은 없느니만 못하다.** 개인정보처리방침에는 운영팀 대리 접속을 반드시 명시해야 한다(`tenant-plan.md` §6.3 이 이력 공개의 근거로 삼은 고지 의무). 정식 공개 전 필수. 사용자 조치 필요 |
| **사업자 정보 미기재** | `tenant/src/shared/config/company.ts` | 2026-08-04 | 상호·대표자·사업자등록번호·통신판매업 신고번호·주소가 비어 있다. **전자상거래법상 표기 의무**다. 값을 지어내지 않고 빈 문자열로 뒀으며, 푸터는 빈 항목을 그리지 않는다. 채우면 자동으로 나타난다. 사용자 조치 필요 |
| **R2 자격증명 폐기·재발급** | `docs/r2-info.md` | 2026-08-04 | Access Key ID·Secret Access Key·API Token 이 평문으로 한 번 커밋됐다(`git add -A` 에 쓸림). **원격이 없어 이 PC 밖으로 나가지 않았고**, 커밋 재작성 + reflog 만료 + gc 로 객체 DB 에서 제거했다. 그래도 **폐기·재발급이 안전하다** — Cloudflare 대시보드에서 R2 API 토큰을 새로 만들고 기존 것을 지운다. 새 값은 `.env` 에만 둔다. 사용자 조치 필요 |
| **현재 Gemini 키가 대화 기록에 노출됐다** | 콘솔 등록분 (`provider_credentials`) | 2026-08-05 | 연동에 쓴 키를 채팅으로 전달받았다 — **그 기록에 평문으로 남아 있다.** DB 에는 암호화돼 있고 서버 로그·응답에도 안 나가지만, 전달 경로가 이미 노출됐다. Google AI Studio 에서 폐기 후 재발급하고 **운영 콘솔 → 모델과 프롬프트 → 공급사 연결**에서 교체할 것(재배포 불필요, 30초). 사용자 조치 필요 |
| **운영 콘솔 IP 제한이 앱에 없다 — 설정만 있고 강제가 없다** | `AppProperties.Ops#ipAllowlist` | 2026-08-05 | `application-production.yml` 이 `OPS_IP_ALLOWLIST` 를 **필수로 요구**해(없으면 기동 실패) 제한이 걸린 것처럼 보이지만, **그 값을 읽는 코드가 어디에도 없다.** 기획서 §8 은 운영 콘솔을 사내 IP·VPN 뒤에 두라고 정했는데 지금은 로그인만 통과하면 인터넷 어디서나 들어온다. 배포하면 이 간극이 실제 노출이 된다. 임시 방어는 `deploy/apache/dabhaejwo-mng.conf` 의 `Require ip` 블록(주석 처리돼 있으니 **반드시 풀 것**). 제대로 고치려면 `/api/ops/**` 필터 체인에 IP 검사를 넣는다 — 그래야 프록시를 우회해도 막힌다 |
| 노출됐던 Gemini API 키 폐기·재발급 | 구 `ai-info.md` | 2026-08-03 | 문서에서는 제거했고 `.env` 값도 비워졌으나 **키 자체는 아직 유효할 수 있다.** Google AI Studio 에서 폐기 후 재발급. 새 키는 이제 `.env` 가 아니라 **운영 콘솔 → 모델과 프롬프트 → 공급사 연결**에서 등록한다(2026-08-04, M6) — 암호화 저장되고 재배포 없이 교체된다. 사용자 조치 필요 |
| **요금제별 서비스 수가 정해지지 않았다** | `plans.bot_limit` | 2026-08-14 | 서비스 다중화(M3)를 붙이면서 상한 컬럼을 만들었지만 **값은 전부 1로 두고 정책을 미뤘다** — 백필 직후 아무도 위반 상태가 되지 않게 하려면 그것이 유일하게 안전한 값이었고, 요금제별 개수는 마이그레이션에 박을 것이 아니라 가격 결정이기 때문이다. 지금은 **테스트용으로 스타터만 3**이고 나머지는 1이라, **비즈니스(89,000원)가 스타터(39,000원)보다 서비스를 적게 만들 수 있는 상태다** — 이대로 판매하면 그 자체가 문의가 된다. 운영 콘솔 → 요금제에서 바꾼다(2026-08-14 에 입력칸을 붙였다). 판매 개시 전 필수. 사용자 결정 필요 |

## P1

| 항목 | 위치 | 등록일 | 비고 |
|------|------|--------|------|
| **질문이 큰 조각에 묻히면 답을 못 한다** | `Chunker` · `answer_fail_similarity` | 2026-08-05 | 배송·반품·A/S·결제가 한 문서에 있고 조각이 1개로 나왔더니, "결제 수단은 뭐가 있나요?"가 임계값을 못 넘어 답변 실패했다(문서에 답이 **있는데도**). 조각이 크면 주제가 섞여 특정 질문과의 유사도가 희석된다. 조각 크기(600자)를 줄이거나 문단 단위로 더 잘게 나눠야 한다. 지금 동작은 **지어내지 않고 거절**하는 쪽이라 안전하지만, 업체는 "문서에 있는데 왜 못 답하지"로 겪는다 |
| 생각 토큰 예산이 코드 상수다 | `GoogleLlmProvider.THINKING_BUDGET` | 2026-08-05 | 0(끔)으로 고정했다 — 실측에서 생각 464 / 답변 34 토큰이 나왔고, 꺼도 품질이 같았다. 공급사마다 개념이 달라 `cost_guards` 가 아니라 어댑터에 뒀다. 추론이 필요한 업체(복잡한 규정 해석 등)가 생기면 요금제별 설정으로 올린다 |
| Gemini Interactions API 로 이전 검토 | `GoogleLlmProvider` | 2026-08-04 | 공식 문서가 신규 연동에 Interactions API(`/v1beta/interactions`)를 권장한다. `generateContent` 는 아직 동작하지만 legacy 로 밀리는 중이다. 지금 안 쓴 이유는 레퍼런스에 **키 전달 방식이 명시돼 있지 않아** 형상을 확신할 수 없었기 때문이다. 어댑터가 한 클래스라 교체 비용은 작다 |
| 마스터 키 교체 경로가 없다 | `SecretCipher` · `provider_credentials` | 2026-08-04 | `ENCRYPTION_KEY` 를 바꾸면 저장된 공급사 키를 복호화할 수 없어 전부 다시 등록해야 한다(그때 `CREDENTIAL_UNREADABLE` 로 명시적으로 실패한다). 키 회전이 필요해지면 구키·신키를 함께 들고 재암호화하는 절차가 필요하다 |
| **PostgreSQL null 파라미터 함정이 룰에 없다** | `.claude/rules/backend-spring-boot.md` | 2026-08-04 | `[harness]` 이번 마일스톤에서 **같은 함정에 두 번 빠졌다.** ① JPQL 함수 안에만 나오는 String 파라미터에 null 을 주면 PG 가 `bytea` 로 바인딩해 `function lower(bytea) does not exist` 로 터진다 → 빈 문자열을 넘긴다. ② `:from IS NULL OR col >= :from` 형태의 timestamptz 파라미터는 타입 추론 근거가 없어 `could not determine data type of parameter` 로 터진다 → null 을 받지 않고 호출부가 기본 범위를 정한다. **컴파일도 단위 테스트도 잡지 못하고 실 DB 호출에서만 드러난다.** `KnowledgeDocumentRepository` 주석에 ①이 이미 적혀 있었는데도 새 쿼리에서 되풀이했다 — 주석이 아니라 룰에 있어야 한다. PG 고유 규칙 절에 추가하고 팩토리로 환류 |
| 팩토리 Spring Boot 모듈이 MySQL을 고정 | `E:\_agent\stacks\backend-spring-boot.md` | 2026-08-03 | `[harness]` 모듈이 "관계형 DB"를 MySQL로 못박아 PG 프로젝트마다 치환이 필요하다. `_guide.md` 규칙 4(버전 하드코딩 금지)의 DB판. DB 선택을 기획서에 위임하도록 일반화 |
| 팩토리 FSD 모듈에 특정 프로젝트 사정이 섞임 | `E:\_agent\skills\fsd-rules.md` | 2026-08-03 | `[harness]` 상단 주의에 "ScoreShot admin 은 TanStack·Zustand 미설치"라는 **그 프로젝트만의 상태**가 재사용 모듈 본문에 들어 있다. 재사용 모듈은 스택 규칙만 담아야 한다 |
| 스택 모듈 선택 기준 중복 | `E:\_agent\stacks\` | 2026-08-03 | `[harness]` `admin-next-console.md` 와 `frontend-rules.md` 의 적용 조건이 겹쳐(둘 다 관리자 화면 있는 Next 앱) 판단이 어렵다. 전자는 "외부 UI 라이브러리 금지", 후자는 허용이라 결과가 크게 갈린다 |
| widget 스택 모듈 팩토리 미저장 | `.claude/rules/widget-embed-script.md` | 2026-08-03 | `[harness]` 신규 작성했으나 아직 `E:\_agent\stacks\` 에 없다. `_guide.md` 규칙 2에 따라 환류 필요 |
| **Docker 미설치 — 통합 테스트 불가 (확정)** | `api/` | 2026-08-03 | 로컬에 Docker 가 없어 Testcontainers 를 못 쓴다. 원가 계산은 순수 단위 테스트(`ModelPriceLookupTest`)로 커버했으나, **Flyway 마이그레이션·JPA 매핑·벡터 검색은 아직 한 번도 실행되지 않았다.** Docker 확보 후 `@SpringBootTest` + Testcontainers(`pgvector/pgvector:pg17`)로 검증할 것. **H2로 때우지 않는다** |
| **DB 클러스터가 공유 자산이고 `pg_hba` 가 전면 개방** | DB 서버 `192.168.0.254:6001` | 2026-08-04 | `pg_hba.conf` 마지막 줄이 `host all all 0.0.0.0/0 md5` 다 — 포트만 닿으면 **어느 IP 에서든 아무 DB 에나** 인증을 시도할 수 있고, `md5` 는 구식이다. 같은 클러스터에 다른 프로젝트(`mbeauty`)가 있어 함부로 못 고친다. 지금은 6001 이 공유기에서 막혀 있어 실害가 없지만, **운영 개시 전 필수**: catch-all 을 걷어내고 `scram-sha-256` + 소스 IP 로 좁힌다. 먼저 `pg_stat_activity` 로 기존 접속 IP 를 확인할 것. `local all postgres` 를 `peer` 로 바꿔 둔 상태이며 원본은 `pg_hba.conf.bak` |
| 임시로 바꾼 `pg_hba.conf` 원복 검토 | DB 서버 | 2026-08-04 | `postgres` 계정 접근을 위해 `local all postgres md5` → `peer` 로 바꿨다. `peer` 가 더 안전하므로 그대로 두어도 되지만, `mbeauty` 쪽 운영 스크립트가 비밀번호 접속에 의존한다면 깨진다. 확인 필요 |
| **운영자 계정을 만들 경로가 없다 (운영)** | `api` `OpsDemoSeeder` | 2026-08-04 | 운영자 시드는 `local` 프로파일에서만 돈다. 운영에 배포하면 `operators` 가 비어 있어 **아무도 콘솔에 로그인할 수 없다.** 지금은 DB 에 직접 넣어야 한다. 초대 흐름이나 SSO 가 붙기 전까지는 부트스트랩용 CLI 나 마이그레이션 한 줄이 필요하다. `operators.password_hash` 는 V4 에서 추가됨 |
| **오늘 화면의 시스템 지표가 미측정** | `TodayService#system` | 2026-08-04 | `chatApiP95Ms`·`crawlerWorkers`·`vectorDbUsagePercent`·`todayError5xxCount` 는 잴 곳이 없어 `null` 로 내려간다(화면은 "집계 없음"). **0으로 채우지 않은 것이 의도다** — 0이면 운영자가 정상이라고 읽는다. 답변 파이프라인·워커·APM 이 붙을 때 함께 채운다. `embedQueueDepth` 만 `jobs` 에서 실측 |
| **집계 테이블의 하루 경계가 아직 UTC 다** | `DailyUsageAggregator` · `AiUsageQueryService` | 2026-08-05 | 화면과 한도 판정은 `BusinessDay`(Asia/Seoul)로 옮겼지만, `tenant_daily_usage` 를 만드는 배치와 AI 사용량 조회는 아직 `ZoneOffset.UTC` 로 하루를 끊는다. **두 기준이 섞여 있어 같은 날의 숫자가 화면마다 다를 수 있다.** 함께 옮기지 않은 이유는 이미 쌓인 행의 `usage_date` 의미가 바뀌기 때문이다 — 옮길 때 과거 구간을 재집계해야 한다(배치가 덮어쓰는 방식이라 가능하다) |
| **자동 청구에 다중 인스턴스 잠금이 없다** | `BillingWorker` | 2026-08-06 | 인스턴스가 여럿이면 같은 업체를 둘이 집어간다. **돈이 두 번 나가지는 않는다** — 주문번호가 결정적이고, 토스 멱등키가 있고, `billing_records.order_id` 에 UNIQUE 가 있다. 다만 헛된 API 호출과 실패 로그가 쌓인다. 확장 시 `SELECT ... FOR UPDATE SKIP LOCKED` 나 ShedLock 을 붙인다 |
| ~~자동 청구 배치가 없다~~ | ~~`BillingService#charge`~~ | 2026-08-06 | **완료** — 이전 내용: 결제 실행 경로는 완성됐지만 **매달 부르는 스케줄러가 없다.** 지금 상태로는 카드를 등록해도 아무도 청구되지 않는다. 붙일 때 함께 정해야 할 것 — 청구일(매월 1일? 가입일 기준?), 실패 시 재시도 간격과 횟수, 연속 실패 시 `SUSPENDED` 전환 여부, 실패 알림(메일). 스케줄러는 다중 인스턴스 잠금도 필요하다(`SchedulingConfig` 항목 참조) — **결제는 두 번 도는 순간 이중 청구다** |
| 토스 웹훅을 받지 않는다 | `TOSS_SECURITY_KEY` | 2026-08-06 | 보안 키를 설정에 넣었지만 웹훅 엔드포인트가 없다. 카드 자동 취소·정기결제 상태 변경이 토스 쪽에서 일어나도 우리는 모른다. 서명 검증(HMAC)과 함께 붙인다 |
| 결제 취소·환불 경로가 없다 | `PaymentGateway` | 2026-08-06 | 잘못 청구했을 때 되돌릴 방법이 코드에 없다 — 토스 콘솔에서 수동으로 해야 하고, 그러면 `billing_records` 와 어긋난다. `POST /v1/payments/{paymentKey}/cancel` 을 붙이고 상태를 `REFUNDED` 로 남긴다. 정산 화면은 `REFUNDED` 를 **읽을 준비가 끝나 있다**(환불액이 미수금에서 빠지는 것까지 실 DB 로 확인) — 만드는 쪽만 없다 |
| **결제 실패를 지금 다시 시도할 방법이 없다** | 정산 화면 · `BillingWorker` | 2026-08-06 | 실패하면 배치가 **3일 뒤 새벽 4시**에 돌 때까지 기다려야 한다. 업체가 "방금 카드 바꿨다"고 전화해도 운영자가 할 수 있는 게 없다. 재청구 자체는 이미 안전하다 — 주문번호가 업체+청구월로 결정되고 토스 멱등키와 `order_id` UNIQUE 가 걸려 있어 **이중 청구가 구조적으로 불가능**하다. 버튼과 `POST /api/ops/revenue/records/{id}/retry` 만 없다. 붙일 때 사유 필수 + `audit_logs` 를 함께(돈을 움직이는 액션이다) |
| 정산의 월 경계가 두 기준으로 섞여 있다 | `RevenueService` | 2026-08-06 | 청구·가입·해지는 KST(`BusinessDay`), **모델 원가만 UTC** 다. 일부러 그렇게 뒀다 — 원가를 KST 로 끊으면 AI 사용량 화면과 같은 "이번 달 모델 원가"가 두 화면에서 다르게 나온다. 위의 `DailyUsageAggregator` 항목을 KST 로 옮길 때 함께 옮긴다. 월 단위라 경계에 걸리는 금액은 작다 |
| 테스트 키로 붙어 있다 | `.env` `TOSS_SECRET_KEY` | 2026-08-06 | `test_sk_` 로 시작하는 키다. 실제 결제가 일어나지 않는다 — 기동 로그가 "테스트 키"라고 알린다. 운영 배포 전 라이브 키로 교체. **클라이언트 키도 함께 바꿔야 한다**(둘이 짝이다) |
| **연락처 목록의 열람 권한이 기획서와 어긋날 수 있다** | `LeadService#list` | 2026-08-05 | 기획서 §8 은 "연락처 전체 조회"를 소유자·편집만 허용한다(●●○). 구현은 <b>마스킹된 목록은 전 역할에 열려 있고</b> 원문이 나가는 CSV 만 편집 권한으로 막는다. "전체 조회"가 **목록 전체**인지 **연락처 원문**인지 문장이 갈린다. 다만 기획서가 이 행을 "대화 로그 조회(●●●)"와 따로 둔 것은 연락처를 더 좁게 보겠다는 뜻으로 읽힌다 — 이름은 마스킹돼도 개인정보다. **판단 필요**: 목록도 편집 권한으로 올리면 보기만 역할에게 메뉴 하나가 통째로 사라진다(화면도 함께 고쳐야 한다) |
| 초대한 팀원이 로그인할 방법이 없다 | `MemberService#invite` | 2026-08-05 | 초대는 비밀번호 없이 `PENDING` 행만 만든다 — 본인이 수락 링크로 정하는 구조인데 **Mailer 가 미연동이라 링크가 나가지 않는다.** 즉 지금은 소유자 1명만 실제로 쓸 수 있고, 역할 권한(EDITOR·VIEWER)은 화면에서 한 번도 실행되지 않는다. 서버 강제는 DB 로 계정을 활성화해 확인했다(전 항목 통과). Mailer 연동 시 함께 풀린다 |
| **SMTP 앱 비밀번호가 대화에 평문으로 노출됨** | `.env` `MAIL_PASSWORD` | 2026-08-05 | 사용자가 채팅으로 Gmail 앱 비밀번호를 그대로 붙여넣었다. `.env`(gitignore)에만 저장했고 추적 파일에는 없지만, **대화 기록이 남는 곳이면 그 값은 이미 유출된 것으로 봐야 한다.** Google 계정 → 보안 → 앱 비밀번호에서 해당 항목을 삭제하고 재발급할 것 |
| OTP·초대 발송이 요청 스레드를 막는다 | `LoginChallengeService` · `InviteService` | 2026-08-05 | SMTP 왕복이 로그인 응답 시간에 그대로 더해진다. 타임아웃을 5초로 잡아 최악을 제한했지만, 메일 서버가 느리면 로그인이 그만큼 느려진다. **비동기로 빼지 않은 것은 의도다** — 발송 실패를 사용자에게 알려야 하고, 비동기면 실패가 로그에만 남는다. 발송량이 늘면 큐 + 실패 알림으로 바꾼다 |
| 만료된 `login_challenges` 를 지우지 않는다 | `login_challenges` | 2026-08-05 | 로그인할 때마다 한 행씩 쌓이고 지우는 곳이 없다. 보안 문제는 아니지만(만료·소진된 행은 못 쓴다) 테이블이 무한히 자란다. 일 배치로 30일 지난 행을 지우면 된다 |
| 운영자 계정에 이메일 변경 수단이 없다 | `operators` | 2026-08-05 | 로그인 2단계가 붙어 **운영자 이메일이 곧 인증 수단**이 됐는데, 콘솔에서 바꿀 화면이 없다. 주소를 잃으면 DB 를 직접 고쳐야 들어온다 |
| **요금제 배정이 미연동 공급사를 가리킨다** | `plan_model_assignments` 시드 | 2026-08-04 | 시드가 전 요금제를 `GOOGLE` 로 배정했는데 `GEMINI_API_KEY` 가 비어 있다. 그래서 **위젯 질문이 실제로는 `LLM_PROVIDER_UNAVAILABLE`(503) 로 거절된다.** 조용히 STUB 으로 떨어뜨리지 않은 것이 의도다 — 그러면 운영에서 공급사가 끊겼을 때 아무도 모른다. 공급사를 붙이면 저절로 풀린다. 검증할 때는 배정을 STUB 으로 잠깐 돌려 확인했고 시드 값으로 복구했다 |
| **방문자 레이트 리밋이 인스턴스 메모리에만 있다** | `ChatGuard` · `SlidingWindowLimiter` | 2026-08-04 | 서버가 여러 대면 대당 한도가 되고 재시작하면 초기화된다. 가입 제한과 같은 한계다. 확장 시 Redis 로 함께 옮긴다 |
| 일일 원가 상한이 "이미 쓴 금액"으로만 판정된다 | `ChatGuard#requireWithinDailyCost` | 2026-08-04 | 호출 전에 원가를 알 수 없어 상한을 <b>살짝 넘겨서</b> 멈춘다. 한 번의 초과분은 무시할 만하지만, 조각 수가 큰 요금제에서 한 번의 초과가 커지면 예상 원가를 미리 빼는 방식이 필요하다 |
| 저장 답변에 `latency_ms` 를 남기지 않는다 | `WidgetChatService#answerFaq` | 2026-08-04 | 모델을 안 거치므로 잰 값이 DB 읽기 시간뿐이다. 평균 응답 시간에 섞으면 지표가 실제보다 좋아 보인다. null 로 두어 집계에서 빠진다 — 의도된 선택이지만, "저장 답변 비율"을 따로 보여줄 때 이 사실을 화면에 적어야 한다 |
| **학습 워커가 단일 인스턴스를 전제한다** | `IndexingWorker` | 2026-08-04 | `PENDING` 문서를 조회해 처리한다. 인스턴스가 둘이면 **같은 문서를 둘이 집어** 임베딩 비용이 두 배로 나가고 조각이 중복 삽입될 수 있다. 상태를 `PROCESSING` 으로 먼저 바꿔 창을 좁혔을 뿐 잠금이 아니다. 확장 시 `SELECT ... FOR UPDATE SKIP LOCKED` 나 `jobs` 테이블 기반 큐로 바꾼다 |
| **학습 실패에 자동 재시도가 없다** | `IndexingWorker` · `DocumentIndexer` | 2026-08-04 | 저장소·공급사 오류처럼 다시 하면 될 실패도 `FAILED` 로 끝난다. 업체가 "실패분 다시 학습"을 눌러야 재개된다. `indexing_failed` 는 자동 재시도(지수 백오프·횟수 상한) 대상이지만, 재시도 횟수를 담을 컬럼이 없어 미뤘다 |
| **20MB 파일을 통째로 메모리에 올린다** | `DocumentUploadService#read` · `DocumentIndexer` | 2026-08-04 | 업로드는 `file.getBytes()`, 학습은 R2 에서 받은 스트림을 PDFBox/POI 가 메모리에 펼친다. 동시 업로드가 몰리면 힙이 터진다. 파일 한도(20MB)와 워커 배치(5건)로 상한은 걸려 있으나, 인스턴스 메모리 설정과 함께 실측이 필요하다 |
| **무중단 배포가 아니다** | `.gitlab-ci.yml` · `deploy/docker-compose.yml` | 2026-08-05 | `docker compose up -d --build` 가 컨테이너를 교체한다. api 는 Flyway + JPA 검증 때문에 재기동에 30~60초가 걸리고 **그동안 전 업체의 챗봇이 답하지 않는다.** 방문자에게는 "일시적으로 답할 수 없습니다"로 보인다. 이미지 태그가 `latest` 하나뿐이라 이미지 단위 롤백도 없다(커밋 revert 후 재빌드가 유일한 경로). 배포가 잦아지면 blue-green 이나 최소한 2인스턴스 + 순차 교체가 필요하다 — 다만 지금은 소켓·워커·레이트 리밋이 단일 인스턴스를 전제해 인스턴스를 늘릴 수 없다(선행 과제) |
| 스케줄러에 다중 인스턴스 잠금이 없다 | `SchedulingConfig` · `DailyUsageAggregator` · `NotificationSweeper` | 2026-08-04 | 인스턴스가 여러 대면 배치가 동시에 돈다. 일 집계는 **다시 계산해 덮는 방식**이라 결과가 같고, 알림 훑기는 `dedupe_key` 유니크 인덱스가 두 번째를 떨어뜨린다 — 지금 있는 두 작업은 안전하다. 다만 **부작용이 되돌릴 수 없는 작업(메일·슬랙·결제)을 붙이는 순간** 잠금(ShedLock 등)이 필요해진다. 중복 억제 키가 없는 알림 종류를 배치에서 발행하면 그때 바로 깨진다 |
| **알림 소켓 세션이 인스턴스 메모리에만 있다** | `NotificationSessionRegistry` | 2026-08-05 | 서버가 여러 대가 되면 A 인스턴스에 붙은 사용자는 B 에서 발생한 알림을 **실시간으로** 못 받는다. 다만 알림 자체는 `notifications` 에 남아 목록·배지에는 보이므로 **잃지는 않는다**(배지 폴링 60초가 최악의 지연). 레이트 리밋·학습 워커와 같은 종류의 한계다. 확장 시 Redis pub/sub 으로 인스턴스 간 전파를 붙인다 |
| **토스페이먼츠 미연동 — 결제가 사람 손으로 흐른다** | `PaymentGateway` stub · `PlanOverviewService#requestUpgrade` | 2026-08-05 | 유료 전환이 **문의 티켓**으로 접수되고 운영자가 수동 처리한다. 그래서 `PAYMENT_RECEIVED` 알림은 아무도 발행하지 않고, "누가 결제했다"와 "새 문의가 왔다"가 지금은 **같은 사건**이다. 테스트 키는 `.env`(`TOSS_SECRET_KEY`·`TOSS_SECURITY_KEY`)에 넣어 뒀고 **코드는 아직 읽지 않는다**. 운영 배포 전 라이브 키로 교체. 붙일 때 함께 해야 하는 것 — 결제창(클라이언트 키 별도) · 웹훅 서명 검증 · `billing_records` 적재 · 실패 시 요금제 미변경 · `PAYMENT_RECEIVED` 발행 |
| **알림이 화면 밖으로 나가지 않는다** | `NotificationEvents` · `Mailer` | 2026-08-05 | 전 알림이 콘솔 안에서만 뜬다. **콘솔을 안 보고 있으면 못 본다** — `GLOBAL_COST_CAP_WARNING`(전 업체 챗봇 정지)이 새벽에 떠도 아침까지 아무도 모른다. `Mailer` 는 붙어 있으나(로그인 코드·초대에 사용) 알림에 연결하지 않았고, 슬랙 `#ops-alert` 는 미구현이다. 최소한 `severity=HIGH` 는 밖으로 내보내야 한다. 운영 개시 전 |
| 운영자 알림 목록을 메모리에서 자른다 | `NotificationQueryService#listForOps` | 2026-08-05 | 대상 역할이 `text[]` 라 조회 조건으로 쓰지 않기로 했고(조인 테이블이 필요해진다), 대신 `(page+1)×size×3` 만큼 넉넉히 읽어 역할로 거른 뒤 자른다. **역할이 못 보는 알림이 그 배수를 넘게 쌓이면 뒤쪽 페이지가 비어 보인다.** 운영자 알림은 건수가 작아 지금은 문제없지만, 종류가 늘고 역할이 좁아지면 조인 테이블로 옮긴다 |
| 대화당 원가 추정 상수가 추정치다 | `ModelPriceService` | 2026-08-04 | `TOKENS_PER_CHUNK=400`·`PROMPT_OVERHEAD_TOKENS=600`·`ANSWER_OUTPUT_TOKENS=300` 은 **측정값이 아니라 자릿수를 가늠하기 위한 상수**다. 요금제별 모델 배정 화면의 "예상 대화당 원가"가 여기 의존한다. 답변 파이프라인이 붙으면 실제 토큰 분포로 다시 잡는다 |
| 대리 접속 세션은 새로고침하면 끊긴다 | `ImpersonationOpsService` · `RefreshTokenCookie` | 2026-08-05 | 로그인·가입에만 리프레시 쿠키를 내려주고 **대리 접속 토큰에는 내려주지 않는다.** 의도된 선택이다 — 운영자가 남의 업체에 들어간 세션이 브라우저에 14일 남아 있으면 안 되고, 30분 만료(`impersonation-ttl-minutes`)가 그 이유다. 다만 대리 접속 중 새로고침하면 그 자리에서 끊기므로, 운영자는 콘솔에서 다시 발급받아야 한다. 필요해지면 수명이 세션 만료와 같은 별도 쿠키를 두는 방법이 있다 |
| 한 이메일이 여러 업체에 속하는 경우 미지원 | `AppAuthService#findByCredentials` | 2026-08-04 | `tenant_members` 의 UNIQUE 가 `(tenant_id, email)` 이라 같은 이메일이 여러 업체에 있을 수 있다. 지금은 비밀번호가 맞는 첫 번째를 고른다. 대행사가 여러 업체를 관리하는 경우가 생기면 로그인 후 업체를 고르는 단계가 필요하다 |
| 가입 레이트 리밋이 인스턴스 메모리에만 있다 | `SignupRateLimiter` | 2026-08-04 | 서버가 여러 대가 되면 대당 한도가 되어 실효가 떨어지고, 재시작하면 초기화된다. 단일 인스턴스인 지금은 충분하다. 확장 시 Redis 로 옮긴다 |
| 가입 시 이메일 인증 없음 | `SignupService` | 2026-08-04 | 가짜 주소로 체험 계정을 반복 생성할 수 있다. 체험 한도(대화 100·문서 30)와 일일 원가 상한, IP 레이트 리밋으로 피해는 제한된다. Mailer 연동 시 함께 넣는다 |
| 도메인 소유 확인 없음 | `SignupService` · `AllowedOriginService` | 2026-08-04 | 서로 다른 업체가 같은 도메인을 등록할 수 있다. 위젯 키가 달라 동작 충돌은 없고, 남의 사이트에 스크립트를 넣을 수 있는 건 그 주인뿐이라 실질 위험은 낮다. 확인 절차는 가입 마찰을 크게 키우므로 문제가 실제로 생기면 넣는다 (`tenant-public-plan.md` §5.3) |
| SpringDoc OpenAPI 미도입 | `api/build.gradle.kts` | 2026-08-03 | 최신 `springdoc-openapi-starter-webmvc-ui` 2.8.6 은 Spring Boot 3.x 대상이라 Boot 4.1 호환이 불확실해 제외했다. 호환 버전 확인 후 추가 |
| 프로토타입 HTML에 doctype·html·head·body 없음 | `docs/prototype/*.html` | 2026-08-03 | quirks mode 로 렌더될 수 있어 "UI 100% 동일" 판정의 기준이 흔들린다. 코드 전환은 표준 모드 기준으로 맞춘다 |
| **해지 업체 데이터를 지우는 배치가 없다** | `Tenant#churn` · `cost_guards.churn_purge_grace_days` | 2026-08-10 | `tenants.purge_after` 는 해지 시 채워지지만 **읽는 코드가 프로젝트 전체에 없다.** 즉 "해지 30일 뒤 벡터·문서를 파기한다"는 약속이 코드에 없고, R2 오브젝트도 그대로 남는다. 개인정보와 학습 문서가 무기한 보관되는 상태다. 서비스 삭제(`service-plan.md` §9)를 만들 때 purge 워커가 두 축(업체 해지 · 서비스 삭제)을 함께 처리하게 해서 같이 갚는다 |
| `quota_overrides` 가 실제 한도에 반영되지 않는다 | `ChatGuard` · `TenantQueryService#detail` | 2026-08-10 | 운영자가 쿼터를 증량하면 **업체 상세 화면의 숫자만 늘고 실제 차단은 그대로다** — `ChatGuard` 는 `plan.getConvLimit()` 원본만 본다. 증량해 줬는데 챗봇이 계속 멈춘다. 화면과 강제가 갈린 상태 |
| 업체 담당자의 행위는 감사 기록에 남지 않는다 | `audit_logs.operator_id` (NOT NULL) | 2026-08-10 | 감사 테이블이 운영자 전용이라 **업체가 스스로 한 되돌릴 수 없는 행위**(문서 삭제, 앞으로는 서비스 삭제)를 기록할 곳이 없다. 3년 보존·수정 불가 테이블이라 나중에 소급해 채울 수 없다 |
| 계약↔코드 불일치 2건 | `api-contracts.md` §2-4 · §3 | 2026-08-10 | ① `q` 가 담당자 이메일도 잡는다고 적혀 있으나 `TenantRepository` 는 안 잡는다 ② `GET /api/app/impersonation/current` 가 계약에 있으나 코드에는 `/me` 의 `impersonation` 필드로만 존재한다. 계약이 진실 공급원이므로 둘 중 하나를 맞춰야 한다 |
| **감사 기록이 업체 삭제를 막는다** | `audit_logs.tenant_id` FK | 2026-08-14 | `audit_logs` 는 3년 불변인데 `tenant_id` 에 FK 가 걸려 있어, 감사 기록이 한 줄이라도 있는 업체는 **행 자체를 지울 수 없다**(`23503`). 해지 업체 purge 를 만들 때 곧바로 부딪힌다 — 자식 데이터만 지우고 업체 행은 `CHURNED` 로 남기거나, FK 를 `ON DELETE SET NULL` 로 바꿔 "지워진 업체의 기록"을 표현할 수 있게 해야 한다. 후자가 감사의 뜻에 가깝다(행위는 있었고 대상만 사라진 것). 서비스 삭제(M4)와 같은 문제다 |

## P2

| 항목 | 위치 | 등록일 | 비고 |
|------|------|--------|------|
| 감사 기록 CSV 내보내기 미구현 | `admin` 감사 기록 화면 | 2026-08-04 | 프로토타입에 "내보내기" 버튼이 있으나 넣지 않았다. 동작하지 않는 버튼을 두느니 없는 편이 낫다. 대상 범위(기간·필터 반영 여부)와 개인정보 마스킹 정책을 먼저 정해야 한다 |
| 요금제 신규 추가 화면 없음 | `admin` 요금제 화면 | 2026-08-04 | `POST /api/ops/plans` 는 있고 `useCreatePlan` 훅도 있는데 화면을 붙이지 않았다. 요금제 신설은 드물고 코드·정렬 순서까지 정해야 해서 수정 모달보다 입력이 많다. 실제로 필요해질 때 붙인다 |
| 업체 목록·수익성이 전 건을 메모리에서 정렬한다 | `TenantQueryService#candidates` | 2026-08-04 | 원가율이 DB 컬럼이 아니라 집계값이라 SQL 로 정렬할 수 없어 전부 읽어 계산·정렬한다. 기준인 업체 500곳까지는 문제없지만 그 이상이면 원가율을 집계 테이블에 물리화하거나 뷰가 필요해진다 |
| 프로토타입 인라인 스타일 대량 | `docs/prototype/*.html` | 2026-08-03 | `style=` 속성 수백 개. `kickoff-prompt.md` §1.1이 복사 금지이므로 전부 토큰으로 재작성해야 한다. 전환 비용이 작지 않음 |
| 프로토타입 접근성 부채 | `docs/prototype/*.html` | 2026-08-03 | 토글이 `div+onclick` 이라 키보드 조작 불가, 업체 목록 행도 `<tr onclick>` 뿐. 재작성 시 radix 기반으로 해소 |
| 위젯 프로토타입이 Google Fonts 로드 | `docs/prototype/chatbot-visitor-widget.html` | 2026-08-03 | 방문자에게 폰트 다운로드를 강제하고 호스트 사이트 LCP에 영향. 실 구현은 system font stack (`widget-embed-script.md`) |
| 프로토타입 위젯 매칭이 글자 겹침 휴리스틱 | `docs/prototype/chatbot-visitor-widget.html` | 2026-08-03 | 데모용. 실 구현은 질문 임베딩 유사도로 판정하며 임계값은 설정값(`answer_fail_similarity`) |
