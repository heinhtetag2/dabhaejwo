"use client";

import { GuideButton, GuideList, GuideSection, GuideWarning } from "@/shared/ui/guide-button";

/**
 * 모델과 프롬프트 화면 사용 가이드.
 *
 * <p>이 화면에는 <b>되돌릴 수 없는 조작</b>이 셋 있다(단가 소급 불가·임베딩 공급사 변경·
 * 원가 상한). 가이드의 대부분이 그 셋을 설명하는 데 쓰인다.
 */
export function ModelsGuide() {
  return (
    <GuideButton title="모델과 프롬프트 — 사용 가이드">
      <GuideSection heading="이 화면이 정하는 것">
        <p>
          챗봇이 <b>어떤 모델로 답하고 그 값이 얼마인지</b>를 정합니다. 전 업체 공통이며,
          운영 관리자만 볼 수 있습니다.
        </p>
      </GuideSection>

      <GuideSection heading="모델 단가 — 고치는 게 아니라 새로 등록합니다">
        <p>
          등록된 단가는 <b>수정도 삭제도 되지 않습니다.</b> 값이 바뀌면 새 행을 적용 시점과 함께
          등록합니다. 과거 행은 읽기 전용 이력으로 남습니다.
        </p>
        <p>
          원가는 <b>호출하던 그 순간의 단가</b>로 계산해 확정 저장하기 때문입니다. 기존 행을
          고칠 수 있으면 지난달 정산이 소리 없이 바뀌고, 언제부터 적자였는지 추적할 수 없게
          됩니다.
        </p>
      </GuideSection>

      <GuideSection heading="요금제별 모델 배정">
        <p>
          어느 요금제가 어떤 모델을 쓸지 정합니다. <b>조각 수</b>는 답변을 만들 때 프롬프트에
          싣는 문서 조각의 개수입니다 — 많을수록 정확해지지만 입력 토큰이 비례해 늘고, 답변
          원가의 대부분이 입력입니다.
        </p>
        <p>단가가 등록되지 않은 모델을 배정하면 그 요금제의 챗봇이 답하지 못합니다.</p>
      </GuideSection>

      <GuideSection heading="비용 안전장치">
        <GuideList
          items={[
            <>
              <b>업체 일일 상한</b> — 한 업체가 하루에 쓸 수 있는 원가입니다. 넘으면 그 업체의
              챗봇만 멈춥니다
            </>,
            <>
              <b>전체 일일 상한</b> — 넘으면 <b>모든 업체의 챗봇이 멈춥니다.</b> 우리 쪽 사고를
              막는 마지막 방어선입니다
            </>,
            <>
              <b>IP 분당 질문 수</b> — 봇이 두드릴 때 폭주를 늦춥니다. IP 는 회사·학교에서
              공유되므로 사람 단위가 아닙니다
            </>,
          ]}
        />
      </GuideSection>

      <GuideSection heading="검색 설정">
        <p>
          <b>답변 실패 유사도</b>는 &quot;근거를 못 찾았다&quot;고 판단하는 기준입니다. 이 값에
          못 미치면 <b>모델을 부르지 않고</b> 실패로 끝냅니다 — 근거 없이 부르면 모르는 것을
          지어내고 그 답에 돈까지 나갑니다.
        </p>
        <p>
          너무 높이면 문서에 답이 있는데도 못 찾고, 너무 낮추면 엉뚱한 문서로 답합니다. 바꾼
          뒤에는 답변 개선 목록에서 실패가 늘었는지 확인하세요.
        </p>
      </GuideSection>

      <GuideSection heading="공급사 연결">
        <p>
          API 키를 등록합니다. 저장된 키는 <b>다시 볼 수 없고</b> 앞뒤 몇 글자만 표시됩니다.
          바꾸려면 새로 등록합니다. 사유가 필요하며 감사 기록에 남습니다(키 자체는 남지 않습니다).
        </p>
      </GuideSection>

      <GuideWarning>
        <b>임베딩 공급사는 함부로 바꾸지 마세요.</b> 바꾸는 순간 이미 학습된 조각이 전부
        무효가 됩니다 — 다른 모델이 만든 벡터끼리는 거리를 비교할 수 없기 때문입니다. 전 업체가
        문서를 다시 학습해야 하고 그만큼 원가가 다시 나갑니다. 그래서 다른 설정과 저장 버튼을
        나눠 두었습니다.
      </GuideWarning>
    </GuideButton>
  );
}
