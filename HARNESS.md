# HARNESS — 프로젝트 운영 규칙 (스택 무관)

세션이 바뀌어도 일관되게 작업하기 위한 규칙. 모든 세션은 이 문서와 CLAUDE.md를 기준으로 동작한다.

## 세션 시작 시
1. `CLAUDE.md`의 마일스톤/결정/Stub 목록을 읽는다.
2. `docs/IMPROVEMENTS.md`에 P0(블로킹)가 있으면 새 기능보다 먼저 처리할지 사용자에게 알린다.
3. 진행 중 마일스톤이 있으면 거기서 이어서 작업한다.

## 마일스톤 규칙
- 작업은 마일스톤 단위로 묶는다. 하나의 마일스톤 = 검증 가능한 결과물 (이 프로젝트의 검증 명령 통과).
- 마일스톤 완료 시 `CLAUDE.md` 마일스톤 섹션에 한 줄 기록 (날짜 포함, 최신이 위).
- 기능 마일스톤은 **세로 슬라이스**로: 하나의 기능을 데이터 → 로직 → 인터페이스(API/화면/CLI) → 테스트까지 끝까지. 레이어별 가로 진행 금지.

## 검증 게이트 (마일스톤 완료 조건)
- 검증 명령(빌드/테스트/린트)은 `CLAUDE.md > 검증 명령` 섹션에 프로젝트 생성 시 명시한다. 각 스택 모듈이 기본 명령을 정의한다.
- 검증이 깨진 상태로 마일스톤 완료 기록 금지.

## 세션 간 상태 기록
- 결정은 `CLAUDE.md > 핵심 결정`에, 미루는 건 `docs/IMPROVEMENTS.md`에, 미구현 외부 의존성은 `CLAUDE.md > Stub 목록`에. 머릿속이나 대화에만 남기지 않는다.
- 작업 중단 시(미완성 상태로 세션 종료) 마일스톤 섹션에 `진행중:` 으로 현재 위치를 남긴다.

## 진화 루프 (evolve)
- 작업 중 rules가 틀렸거나/부족했거나/이 프로젝트와 안 맞아서 어겼다면, 그 사실을 `docs/IMPROVEMENTS.md`에 `[harness]` 태그로 기록한다.
- 마일스톤 종료 시점에 `[harness]` 항목이 있으면 사용자에게 보고하고, 승인되면 `E:\_agent`(팩토리)의 해당 모듈에 반영한다. 프로젝트에서만 고치고 팩토리를 안 고치면 다음 프로젝트가 같은 문제를 겪는다.
